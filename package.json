{
	"name": "meme",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "firebase use default && next dev",
		"build": "npm run lint && next build && next export",
		"start": "next start",
		"lint": "next lint",
		"emulator": "firebase emulators:start --import ./firebase-seed --export-on-exit",
		"deploy": "npm run deploy:dev && npm run deploy:stage && npm run deploy:prod",
		"deploy:dev": "npm run build && firebase use default && firebase deploy --only hosting",
		"deploy:stage": "npm run build && firebase use prod && firebase hosting:channel:deploy stage",
		"deploy:prod": "npm run build && firebase use prod && firebase deploy --only hosting"
	},
	"dependencies": {
		"@next/font": "13.1.6",
		"@reacticons/ionicons": "^6.1.1",
		"@types/node": "18.13.0",
		"@types/react": "18.0.28",
		"@types/react-dom": "18.0.10",
		"eslint": "8.34.0",
		"eslint-config-next": "13.1.6",
		"firebase": "^9.17.1",
		"next": "13.1.6",
		"react": "18.2.0",
		"react-dom": "18.2.0",
		"react-helmet-async": "^1.3.0",
		"styled-components": "^5.3.6",
		"styled-reset": "^4.4.5",
		"typescript": "4.9.5"
	},
	"devDependencies": {
		"@types/styled-components": "^5.1.26"
	}
}
