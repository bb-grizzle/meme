import styled from "styled-components";

const Text = styled.p`
	font-weight: 900;
	font-size: 24px;
	font-style: italic;
`;

const Logo = () => {
	return <Text>me?ME!</Text>;
};

export default Logo;
